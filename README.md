Цей репозиторій — це моя реалізація інфраструктури для Python REST API.
Я сфокусувався на підході **Infrastructure as Code (Terraform)** та повній автоматизації розгортання, замість ручного написання YAML-маніфестів.


## 🛠 Технологічний Стек

* **App:** Python (Flask) + Gunicorn (реалізовано Graceful Shutdown).
* **DB:** PostgreSQL 16 (розгорнуто через **Helm Chart** від Bitnami).
* **IaC:** Terraform (керує всім: Deployment, Service, Ingress, Secrets, HPA).
* **Orchestration:** Minikube (локальний кластер).
* **Scaling:** Horizontal Pod Autoscaler (HPA) на базі CPU.

---

## ⚡️ Quick Start (Запуск за 1 хвилину)

Я написав скрипт автоматизації для Windows (`manage.ps1`), щоб не вводити купу команд вручну.

### 1. Вимоги
* Docker, Minikube, Terraform.

### 2. Запуск
```powershell
# Розгорнути все (Infa + App + DB)
.\manage.ps1 deploy
3. Перевірка (Verification)
Оскільки локально у нас немає справжнього DNS, я використовую емуляцію заголовка Host. Це перевіряє весь ланцюжок: Ingress -> Service -> Pod -> DB.

# Перевірити статус API та підключення до БД
Invoke-WebRequest -Uri "[http://127.0.0.1/api/v1/status](http://127.0.0.1/api/v1/status)" -Headers @{Host="my-api.info"}
Очікуваний результат: 200 OK та JSON з конфігурацією БД.

💡 Архітектурні Рішення (Що зроблено і чому)
Тут я хочу пояснити різницю між цим тестовим середовищем і реальним Production.

1. Моніторинг
Local: Я використав Metrics Server + Minikube Dashboard. Це легко для ноутбука і дозволяє працювати HPA.

Production Blueprint: У файлі infrastructure/monitoring.tf я підготував (але закоментував) код для розгортання Kube Prometheus Stack, в цілому для роботи умовно локально достатньо метрик з Minikube 

2. DNS
Local: Використовується локальний тунель та hosts мапінг.

Production Blueprint: У файлі infrastructure/dns.tf є чернетка для ExternalDNS, який у реальному житті автоматично створював би записи в Cloudflare/AWS Route53.

3. Безпека
Secrets: Паролі не в коді. Вони йдуть через secrets.tfvars -> K8s Secret (хоча в хмарах є більш спеціалізовані і з точки зору безпеки правильні рішення) цей файл і інше навмисно не були внесенні в гітігнор .

Non-root: Контейнери налаштовані через security_context (заборона root).

Network: База даних захована всередині кластера (ClusterIP).

Roadmap: Для ідеалу не вистачає NetworkPolicy (щоб ізолювати трафік між подами), але це вже трохи за рамки тестового, в цілому можна сказати що 
